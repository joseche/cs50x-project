ScreenWidth = 1200
ScreenHeight = 800
SquareSize = math.floor((ScreenHeight * 0.9) / 8)

BoardWidth = SquareSize * 8

SpritePieceSize = 200 -- this doesn't change
PieceScaleFactor = SquareSize / SpritePieceSize
